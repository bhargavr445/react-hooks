const OutcomesRoster = ({rosterData}) => {
    console.log(rosterData);
    return (
        <div>
            Roster Component..
            {/* {rosterData.toStringify()} */}
        </div>
    );
}

export default OutcomesRoster;